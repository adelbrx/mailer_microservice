class Sender {
  constructor(username, email, password, host, port) {
    this.username = username;
    this.email = email;
    this.password = password;
    this.host = host;
    this.port = port;
  }
}

module.exports = Sender;
